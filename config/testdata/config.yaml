yggdrasil:
  client:
    example.polaris.server:
      grpc:
        target: 127.0.0.1:30001

  logger:
    handler:
      default:
        type: "json"
        writer: "file"
        config:
          level: "debug"
          addSource: true
    writer:
      file:
        type: "file"
        filename: "/tmp/yggdrasil.log"
        maxSize: 10

TestTag:
  data: "test"
